import objetos.*

describe "aa" {
    method initialize() {
        game.width(15)
        game.height(10)
    }
  test "inicio" {
        
        assert.notEquals{game.at(0,5), pelota.position()
        }
        pelota.inicio()
        assert.equals{game.at(0,5), pelota.position()

    }
}
    